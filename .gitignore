# Abhängigkeiten
node_modules/

# Builds
dist/
build/
client/dev-dist/

# Umgebungsvariablen — enthält den API-Key, niemals committen
.env
.env.*
!.env.example
*.local
# Versehentlich abgelegte Schlüssel-/Token-Dateien
sk-ant-*.txt
*api*key*.txt
*key*.txt
*token*.txt
*secret*.txt

# PRIVAT: mila.png ist ein privates Foto — bleibt lokal, wird nie veröffentlicht
# (die App zeigt online automatisch einen Platzhalter; siehe docs/charakter-portraits-anleitung.md)
client/public/characters/mila.png

# Planungsunterlagen bleiben lokal (privates Foto in docs/assets + Budget-/Businessnotizen).
# Bei Bedarf gezielt einzelne Dateien wieder aufnehmen (git add -f docs/...).
docs/
CLAUDE_CODE_START_PROMPT.md
PROJEKT_PROMPT_Language_Teacher_APP.md
Screenshot*.png

# Laufzeit-Artefakte des Dev-Setups
.claude/scheduled_tasks.lock

# Lokale libSQL/SQLite-Datei-DB (nur Dev; Produktion nutzt Turso)
*.db
*.db-shm
*.db-wal
server/local-accounts.db*

# Lokale Backups (enthalten docs/ mit privaten Dateien)
backup/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
