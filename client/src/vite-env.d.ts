/// <reference types="vite/client" />

// Build-Kennung aus vite.config.ts (define) — sichtbar im Header,
// damit man am Handy prüfen kann, welche Version wirklich läuft
declare const __BUILD_ID__: string
